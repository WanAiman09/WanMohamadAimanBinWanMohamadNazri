const email = document.getElementByEmail('email')
const name = document.getElementByEmail('password')
const form = document.getElementByEmail('form')

if (password.value.length <=5){
  messages.push(Password must contain atleast 6 characters )
}
